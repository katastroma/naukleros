syntax = "proto3";

package naukleros;

option go_package = "github.com/katastroma/naukleros";

// RetrieverService is the API contract for source handlers.
service RetrieverService {
  // Retrieve allows manual source retrieval dispatch
  rpc Retrieve(RetrieveRequest) returns (RetrieveResponse);
  // Replay a source retrieval from a previous run
  rpc Replay(ReplayRequest) returns (ReplayResponse);
}

message RetrieveRequest {
  string namespace = 1;
  string watch_target_id = 2;
}

message RetrieveResponse {}

message ReplayRequest {
  string run_id = 1;
}

message ReplayResponse {}
